<table border="1">
<tr>
  <th>First Name:</th>
  <th>Last Name:</th>
  
  <th>Phone Number:</th>
</tr>
  <% @contact.each do |contact| %>
  <tr>
    <td><%= contact.first_name %></td>
    <td><%= contact.last_name %></td>
    <td><%= contact.phone_number %></td>
  </tr>
  <% end %>
</table>
